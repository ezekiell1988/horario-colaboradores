BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Colaborador] ALTER COLUMN [turnoFijo] NVARCHAR(10) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
